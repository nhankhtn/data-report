export interface ITransaction {
  time: string;
  quantity: string;
  pillar: string;
  revenue: string;
  price: string;
}
export interface IErrors {
  time?: string;
  quantity?: string;
  pillar?: string;
  revenue?: string;
  price?: string;
}
